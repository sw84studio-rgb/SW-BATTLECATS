# SW BATTLECATS V039 판본 데이터 정책

## 모바일
- 현재 canonical 데이터: **KR 15.5.0**.
- 캐릭터 / 적 / 스테이지 / 특수 범주는 기존 V038 데이터를 유지합니다.

## Nintendo Switch
- 공개 자료 대조는 시작했지만, 현재 FULL 원본 안에는 Switch canonical 데이터셋이 없습니다.
- 모바일 데이터를 Nintendo 데이터로 복제하지 않습니다.
- 캐릭터/적/스테이지/특수 범주를 **각각 독립적으로 available** 처리할 수 있게 V039 메타데이터를 확장했습니다.
- M/N 공통·전용 표시는 항목별 교차검증이 끝난 뒤에만 생성합니다.

## 스테이지 이미지
- `배경(background)`과 `적 성(enemy base/castle)`을 서로 다른 자산으로 관리합니다.
- `platform + region + entry_id` 기준 검증을 통과한 이미지만 표시합니다.
- 이미지가 없으면 기존 V038처럼 빈 placeholder를 만들지 않습니다.
- 일본/대만 스테이지 순서를 근거로 한국판 성 이미지를 추측 매핑하지 않습니다.
