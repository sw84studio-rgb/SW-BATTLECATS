# SW BATTLECATS V037 GitHub Pages 업로드본

이 폴더의 내용 전체를 GitHub 저장소 root에 업로드한다.

- Pages 설정 변경 없음: `main / (root)` 유지
- SQL 없음
- V037은 큰 원본 JSON 대신 `data/runtime-v037/*.jgz` 압축 런타임을 사용한다.
- 기존 V035/V036의 큰 `data/*.json` 파일이 저장소에 남아 있어도 V037 화면은 더 이상 읽지 않는다.
- Nintendo판은 현재 프로젝트 원본 미확보 상태이며 모바일 데이터를 대신 보여주지 않는다.
- 캐릭터 그림은 검증된 공개 경로 fallback, 적은 explicit-only. 스테이지/특수 아이템 실제 그림은 아직 원본 미확보.
